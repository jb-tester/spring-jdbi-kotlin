create table persons(
                        id     INT PRIMARY KEY,
                        name VARCHAR(50),
                        age INT
);
create table contacts(
                        id     INT PRIMARY KEY,
                        name VARCHAR(50),
                        email VARCHAR(50)
);